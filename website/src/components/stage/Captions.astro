---
import type { Beat } from '../../stage/beats.ts';

interface Props {
  beat: Beat;
}

const { beat } = Astro.props;
---

<div class="cap">
  <h3>{beat.heading}</h3>
  {beat.paragraphs.map((p) => <p>{p}</p>)}
</div>
